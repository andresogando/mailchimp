// import { parse } from 'csv-parse'
// import { Contact } from '../interfaces/contact.interface'

// const csvString = 0 // get CSV string from file upload

// const contacts = await new Promise<Contact[]>((resolve, reject) => {
//   parse(csvString, { columns: true }, (err, records) => {
//     if (err) {
//       reject(err)
//     } else {
//       resolve(records as Contact[])
//     }
//   })
// })
